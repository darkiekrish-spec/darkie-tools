version: '3.8'

services:
  darkie-suite:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: darkie-suite
    ports:
      - "5000:5000"
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_ADMIN
    network_mode: host
