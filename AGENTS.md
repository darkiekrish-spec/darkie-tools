# Darkie TOOLS project

## Git
- After successfully implementing and testing a feature, commit then push to `origin` automatically.
- GitHub auth is stored globally at `~/.git-credentials` (credential.helper=store) — both accounts are pre-registered, so pushes work without re-entering tokens.
- This repo = account `darkiekrish-spec` (remote URL already embeds the username). Do NOT re-enter or print PATs.